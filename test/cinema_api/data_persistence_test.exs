defmodule CinemaApi.DataPersistenceTest do
  @moduledoc """
  provides functions for testing the CinemaApi.DataPersistence module
  """
  use ExUnit.Case

  test "2 + 2 == 4" do
    assert 2 + 2 == 4
  end
end
